# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# IDE data
.idea/

# testing
/coverage

# next.js
/.next/
/.next-verify/
/out/

# production
/build

# misc
.DS_Store
*.pem
*.zip

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files (all of them, not just *.local — .env itself was committed to
# history before and must never be re-added by an accidental `git add -A`)
.env
.env*
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Playwright MCP scratch artifacts
.playwright-mcp/
/sidebar-final.png
/sidebar-auth-bottom-desktop.png
/study-workspace-guest-sidebar.png
/toolgrid-mobile-375.png

# Local agent/skill tooling (installed per-machine, not shared product code)
.agents/
.claude/
skills-lock.json
